import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  typography: {
    fontFamily: " font-family: 'Poppins', sans-serif;",
  },
  palette: {
    primary: {
      main: "rgba(51, 6, 147, 1)",
    },
    secondary: {
      main: "rgba(138, 83, 255, 1)",
    },
  },
});

export default theme;
